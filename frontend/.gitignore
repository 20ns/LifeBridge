# Frontend-specific .gitignore
# Main .gitignore is at project root

# Create React App specific
# (Most rules are covered by root .gitignore)

# Local environment variables
.env.local
.env.development.local
.env.test.local
.env.production.local
